kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: {{ name }}
  namespace: ocp-virt
spec:
  to:
    kind: Service
    name: {{ name }}
    weight: 100
  port:
    targetPort: 80
  wildcardPolicy: None
